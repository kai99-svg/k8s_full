plugin: amazon.aws.aws_ec2
regions:
  - us-east-1
filters:
  "tag:eks:nodegroup-name": "mynode"
keyed_groups:
  - key: tags.eks:nodegroup-name
    prefix: node
compose:
  ansible_connection: amazon.aws.aws_ssm
