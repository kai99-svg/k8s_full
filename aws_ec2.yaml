plugin: amazon.aws.aws_ec2
regions:
  - us-east-1  # your region
filters:
  instance-state-name: running
  tag:eks:nodegroup-name: my_node 
compose:
  ansible_host: private_ip_address
  ansible_user: ec2-user  # or ubuntu if Ubuntu nodes
